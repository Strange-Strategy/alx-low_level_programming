#include <stdio.h>
/**
 * main - Entry point
 * program to print numbers
 * Return: 0 Always (Success)
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
